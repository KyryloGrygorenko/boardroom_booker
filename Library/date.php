<?php

namespace Library;

class Date
{
    //        dump(strftime ( '%m' , 'January') );



    //        $range=range(0, 12);
    //        dump(in_array("0", $range));

//        //text format of the month
//        $month=$date_time_array['month'];
//        $year=$date_time_array['year'];
    // First day of chosen month in timestamp format
//        $this_month = strftime ('%m', $chosen_month_unix); //digit format of the month

//        foreach ($all_appointments as $appointment){
//            dump($appointment['start_time']);
//            dump(date("t", strtotime("2011-02")));
//        }

}